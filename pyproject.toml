[project]
name = "underwater-gan-web"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pillow",
    "fastapi",
    "uvicorn",
    "python-multipart",
    "onnxruntime"
]

[tool.vercel]
entrypoint = "api_server:app"
